// The LEZ comments here say that the test function should ALWAYS be optimized
/*LEZ
test, 0;
test2, 0;
*/
function test(obj) {
    var b = obj*2+obj + 87;
    c = 91;
    if(3 == b)
       c = a+b;
    b = 24 * 1334 + 434;
    b = c + 32;
    b = c - b;
    return b * 2 / 4;
}
function test2(obj){
    var b = obj*2+obj + 77;
    c = 91;
    if(3 == b)
       c = a+b;
    b = 24 * 1334 + 434;
    b = c + 32;
    b = c - b;
    return b * 2 / 4;
}

  
for(i=1; i < 100; i++)
  test(i);
for(i=1; i < 2000; i++)
  test2(i);

